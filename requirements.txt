httpx>=0.27.0
pystray>=0.19.5
Pillow>=10.2.0
# Claude Desktop oauth:tokenCache decryption (Chromium v10 / DPAPI / Keychain)
pycryptodome>=3.20.0; sys_platform == "win32" or sys_platform == "darwin"
# Windows-only: enables custom app name in toast notifications
# (replaces "Python" source label with "Claude Quota Tray")
windows-toasts>=1.1.0; sys_platform == "win32"

# History storage and chart popup use stdlib only (sqlite3, tkinter).
# Theme detection on Windows uses winreg from the stdlib.
